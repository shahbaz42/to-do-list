<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>To-Do List</title>
  </head>
  <body>

    <% if (dayName == "Sunday" || dayName == "Saturday" ){ %>
      <h1 style="color : green"><%= dayName %> list.</h1>
    <% }else{ %>
      <h1 style="color : red"><%= dayName %> list.</h1>
    <%}%>

  </body>
</html>
